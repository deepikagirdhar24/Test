There is a payload helper with below 3 methods - 

public String resolvePersonId(String input, String naturalPersonObjectId) {
        if (input == null || input.trim().isEmpty() || input.equalsIgnoreCase("EMPTY")) {
            return "";
        } else if (input.equalsIgnoreCase("NATURAL_PERSON_OBJECT_ID")) {
            return naturalPersonObjectId;
        }
        return input;
    }

    public String resolveOwnerId(String input, String containerObjectId) {
        if (input == null || input.trim().isEmpty() || input.equalsIgnoreCase("EMPTY")) {
            return "";
        } else if (input.equalsIgnoreCase("CONTAINER_OBJECT_ID")) {
            return containerObjectId;
        }
        return input;
    }

    public String resolveAddressId(String input, String addressObjectId) {
        if (input == null || input.trim().isEmpty() || input.equalsIgnoreCase("EMPTY")) {
            return "";
        } else if (input.equalsIgnoreCase("ADDRESS_OBJECT_ID")) {
            return addressObjectId;
        }
        return input;
    }

combine them into one ?
