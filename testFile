return (function(){
  var popup = document.querySelector("[class*='popup'], [class*='modal'], [id*='popup'], [id*='kickout']");
  if (!popup) return "Popup not found";
  return popup.innerText || popup.textContent;
})();
