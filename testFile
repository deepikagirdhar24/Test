This is a pyalod - 

    public List<POJOTaxResidence> buildTaxResidences(String input) {
        if (input == null || input.trim().isEmpty()) return null;

        List<POJOTaxResidence> listTaxResidences = new ArrayList<>();
        String[] entries = input.split("\\|");
        for (String entry : entries) {
            String[] parts = entry.split(";");
            POJOTaxResidence tax = new POJOTaxResidence();
            tax.setStrCountry(payloadHelper.normalize(parts[0]));
            tax.setStrTaxNumber(payloadHelper.normalize(parts[1]));
            tax.setStrIdentityType(payloadHelper.normalize(parts[2]));
            listTaxResidences.add(tax);
        }
        return listTaxResidences;
    }

For part[1], I want to generate a random character of 10 size, how to include that ?

similar to below payload - 

    public POJOChqBookProfileRequest buildChqBookProfilePayload(Map<String, String> mapChqBookDetails, ScenarioContext scenarioContext) {
        POJOChqBookProfileRequest chqBookProfile = new POJOChqBookProfileRequest();

        chqBookProfile.setStrUuid(UUID.randomUUID().toString().substring(0, 7));
