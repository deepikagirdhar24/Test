public static WorkRequest buildWorkRequestPayload(Map<String, String> mapWorkRequestDetails) {
    WorkRequest workRequest = new WorkRequest();

    workRequest.setStrBpId(mapWorkRequestDetails.get("bpId"));
    workRequest.setStrType(mapWorkRequestDetails.get("type"));
    workRequest.setStrSubject(mapWorkRequestDetails.get("subject"));
    workRequest.setStrDescription(mapWorkRequestDetails.get("description"));

    String dueDateInput = mapWorkRequestDetails.getOrDefault("dueDate", "FUTURE");
    LocalDate dueDate;

    switch (dueDateInput.toUpperCase()) {
        case "PAST":
            dueDate = LocalDate.now().minusDays(7); // 1 week ago
            break;
        case "TODAY":
            dueDate = LocalDate.now();
            break;
        default:
            dueDate = LocalDate.now().plusWeeks(1); // FUTURE (default)
    }

    workRequest.setStrDueDate(dueDate.toString()); // Format: yyyy-MM-dd
    return workRequest;
}

  Then Send Request to create Work Request with below details
    | bpId        | <bpId>        |
    | type        | <type>        |
    | subject     | <subject>     |
    | description | <description> |
    | dueDate     | <dueDate>     |

Examples:
  | bpId     | type       | subject | description | dueDate |
  | 66329769 | pay_domq   | Subject | Description | FUTURE  |
  | 66329769 | olb_oarprw | Subject | Description | PAST    |
  | 66329769 | olb_oarprw | Subject | Description | TODAY   |
