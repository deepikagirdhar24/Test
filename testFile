    public String resolveId(String input, String resolvedValue) {
        if (input == null || input.trim().isEmpty() || input.equalsIgnoreCase("EMPTY")) {
            return "";
        }

        if (input.equalsIgnoreCase("NATURAL_PERSON_OBJECT_ID") ||
                input.equalsIgnoreCase("CONTAINER_OBJECT_ID") ||
                input.equalsIgnoreCase("ADDRESS_OBJECT_ID")) {
            return resolvedValue;
        }

        return input;
    }

In this function, if it is NA, then return null - add this line
