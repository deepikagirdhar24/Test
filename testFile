  Scenario Outline: Verify the Post Natural Persons API with valid data - <Comments>
    Given Send Request to get Authentication Token for ICOS
    When I generate the JWT Token
    Then Send request to update Natural Person with below details
      | subType       | <subType >    |
      | domiCountry   | <domiCountry> |

Examples:

Add the fields in this SD and add values in Example section.

See screenshot of values - 
