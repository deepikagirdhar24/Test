  Scenario Outline: Verify the Get Work Request API
    Given Send Request to get Authentication Token for ICOS
    When I generate the JWT Token
    Then Send Request to get Work Request
    And I verify the Status Code for Work Request "<statusCode>"
    And I verify the string field "bpId" is "<bpId>" in Work Request response
    And I verify the string field "type" is "<type>" in Work Request response
    And I verify the string field "dueDate" is "<dueDate>" in Work Request response
    And I verify the string field "subject" is "<subject>" in Work Request response
    And I verify the string field "description" is "<description>" in Work Request response

    Examples:
      | bpId      | type       | dueDate | subject | description  | statusCode |
      | 66329769  | olb_oarprw |         | Subject | Description  | 200        |

    @And("I verify the string field {string} is {string} in Work Request response")
    public void iVerifyStringFieldEquals(String jsonPath, String expectedValue) throws Exception {
        String actualValue = JsonHelper.extractStringField(response, jsonPath);
        ExtentCucumberAdapter.addTestStepLog("Expected value at '" + jsonPath + "': " + expectedValue + ", Actual: " + actualValue);
        Assert.assertEquals(actualValue, expectedValue, "Mismatch at: " + jsonPath);
    }

I am using this feature file and want to validate the response, but not sure how to validate the date ?

IN the validation, we need ot do this -attached screenshot from TOSCA
