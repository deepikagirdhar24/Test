@And("I verify the string field {string} follows the date format {string} in Work Request response")
public void iVerifyDateFormat(String jsonPath, String expectedFormat) throws Exception {
    String dateStr = JsonHelper.extractStringField(response, jsonPath);
    DateTimeFormatter formatter = DateTimeFormatter.ofPattern(expectedFormat);
    try {
        LocalDate.parse(dateStr, formatter);
        ExtentCucumberAdapter.addTestStepLog("Date at '" + jsonPath + "' is in correct format: " + dateStr);
    } catch (DateTimeParseException e) {
        Assert.fail("Date at '" + jsonPath + "' is NOT in expected format: " + expectedFormat + ". Actual: " + dateStr);
    }
}

And I verify the string field "dueDate" follows the date format "yyyy-MM-dd" in Work Request response
